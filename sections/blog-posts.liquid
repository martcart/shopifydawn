

{{section.settings.posts-range}}
{% assign blog=blogs[section.settings.blog] %}
{% assign blog = section.settings.new-blog %}
{% unless blog.articles_count==0 %}
{blog.articles_count}
There is at least one blog post
    
    {% for article in blog.articles limit:section.settings.posts-range %}
    <div class="page-width>
    <div class="grid_item medium-up--one-third">
        {{article.image}}
        <a href="{{article.url}}">
        <img srcc="{{article.image | resize: '500'}}"
        </a>
         <a href="{{article.url}}">
        <img srcc="{{article.image | resize: '500'}}"
        </a>
    <img src= "{{article.image | img_url:width:'500x'}} "/>
        {{article.title}}
    </div>
    </div>
    {%endfor%}

{%endunless%}
<!-- Display articles based on range -->



{% schema %}
{
    "name":"New blog post",
    "settings":[
       
        {
            "id":"posts-range",
            "type":"range",
            "label":"Range",
            "min":3,
            "max":12,
             "step":3,
            "default":3
            
        },
         {
            "id":"new-blog",
            "type":blog",
            "label":"blog"
            
            
        }

    ],
    
     "presets":[
        {
            "name":"New Blog Posts",
            "category":"Posts"
        }
     ]
}
{% endschema %}


